/* tslint:disable */
/* eslint-disable */
/**
 * MONEI API v1
 * The MONEI API is organized around REST principles. Our API is designed to be intuitive and developer-friendly.  ### Base URL  All API requests should be made to:  ``` https://api.monei.com/v1 ```  ### Environment  MONEI provides two environments:  - **Test Environment**: For development and testing without processing real payments - **Live Environment**: For processing real transactions in production  ### Client Libraries  We provide official SDKs to simplify integration:  - [PHP SDK](https://github.com/MONEI/monei-php-sdk) - [Python SDK](https://github.com/MONEI/monei-python-sdk) - [Node.js SDK](https://github.com/MONEI/monei-node-sdk) - [Postman Collection](https://postman.monei.com/)  Our SDKs handle authentication, error handling, and request formatting automatically.  You can download the OpenAPI specification from the https://js.monei.com/api/v1/openapi.json and generate your own client library using the [OpenAPI Generator](https://openapi-generator.tech/).  ### Important Requirements  - All API requests must be made over HTTPS - If you are not using our official SDKs, you **must provide a valid `User-Agent` header** with each request - Requests without proper authentication will return a `401 Unauthorized` error  ### Error Handling  The API returns consistent error codes and messages to help you troubleshoot issues. Each response includes a `statusCode` attribute indicating the outcome of your request.  ### Rate Limits  The API implements rate limiting to ensure stability. If you exceed the limits, requests will return a `429 Too Many Requests` status code.
 *
 * The version of the OpenAPI document: 1.12.2
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

// May contain unused imports in some cases
// @ts-ignore
import type { PaymentBillingDetails } from "./payment-billing-details";
// May contain unused imports in some cases
// @ts-ignore
import type { PaymentCustomer } from "./payment-customer";
// May contain unused imports in some cases
// @ts-ignore
import type { PaymentShippingDetails } from "./payment-shipping-details";
// May contain unused imports in some cases
// @ts-ignore
import type { SubscriptionInterval } from "./subscription-interval";
// May contain unused imports in some cases
// @ts-ignore
import type { SubscriptionPaymentMethods } from "./subscription-payment-methods";
// May contain unused imports in some cases
// @ts-ignore
import type { SubscriptionRetrySchedule } from "./subscription-retry-schedule";

/**
 *
 * @export
 * @interface UpdateSubscriptionRequest
 */
export interface UpdateSubscriptionRequest {
  /**
   * Amount intended to be collected by this payment. A positive integer representing how much to charge in the smallest currency unit (e.g., 100 cents to charge 1.00 USD).
   * @type {number}
   * @memberof UpdateSubscriptionRequest
   */
  amount?: number;
  /**
   *
   * @type {SubscriptionInterval}
   * @memberof UpdateSubscriptionRequest
   */
  interval?: SubscriptionInterval;
  /**
   * Number of intervals between subscription payments.
   * @type {number}
   * @memberof UpdateSubscriptionRequest
   */
  intervalCount?: number;
  /**
   *
   * @type {SubscriptionPaymentMethods}
   * @memberof UpdateSubscriptionRequest
   */
  allowedPaymentMethods?: SubscriptionPaymentMethods;
  /**
   * An arbitrary string attached to the subscription. Often useful for displaying to users.
   * @type {string}
   * @memberof UpdateSubscriptionRequest
   */
  description?: string;
  /**
   *
   * @type {PaymentCustomer}
   * @memberof UpdateSubscriptionRequest
   */
  customer?: PaymentCustomer;
  /**
   *
   * @type {PaymentBillingDetails}
   * @memberof UpdateSubscriptionRequest
   */
  billingDetails?: PaymentBillingDetails;
  /**
   *
   * @type {PaymentShippingDetails}
   * @memberof UpdateSubscriptionRequest
   */
  shippingDetails?: PaymentShippingDetails;
  /**
   * The end date of the trial period. Measured in seconds since the Unix epoch.
   * @type {number}
   * @memberof UpdateSubscriptionRequest
   */
  trialPeriodEnd?: number;
  /**
   * The URL will be called each time subscription status changes. You will receive a subscription object in the body of the request.
   * @type {string}
   * @memberof UpdateSubscriptionRequest
   */
  callbackUrl?: string;
  /**
   * The URL will be called each time subscription creates a new payments. You will receive the payment object in the body of the request.
   * @type {string}
   * @memberof UpdateSubscriptionRequest
   */
  paymentCallbackUrl?: string;
  /**
   * If true, the subscription will be paused at the end of the current period.
   * @type {boolean}
   * @memberof UpdateSubscriptionRequest
   */
  pauseAtPeriodEnd?: boolean;
  /**
   * If true, the subscription will be canceled at the end of the current period.
   * @type {boolean}
   * @memberof UpdateSubscriptionRequest
   */
  cancelAtPeriodEnd?: boolean;
  /**
   * Number of intervals when subscription will be paused before it activates again.
   * @type {number}
   * @memberof UpdateSubscriptionRequest
   */
  pauseIntervalCount?: number;
  /**
   * The number of intervals during which the subscription billing cycle will be skipped without altering the subscription status. This is useful when payment for a specific period is received through different methods.
   * @type {number}
   * @memberof UpdateSubscriptionRequest
   */
  skipIntervalCount?: number;
  /**
   *
   * @type {SubscriptionRetrySchedule}
   * @memberof UpdateSubscriptionRequest
   */
  retrySchedule?: SubscriptionRetrySchedule;
  /**
   * Settle the price difference immediately when you change `amount`, `interval`, `intervalCount` or `nextPaymentAt` mid-period. Unused time is credited at the current price. An `amount` change charges the remaining time at the new price and keeps the billing period. An `interval` or `intervalCount` change charges the full new price and restarts the period from now. A `nextPaymentAt` change charges the time from now until the new date and moves the period end to it, so a later date charges and an earlier one refunds.  A positive difference is charged to the saved payment method. A negative difference is refunded against the payments that funded the current period, newest money first: the charge taken by the most recent prorated change in this period, then the payment that renewed it. A refund those together cannot cover is rejected before any of it moves, so a change is either settled in full or not at all. The same rule applies to the preview, which returns the same rejection rather than quoting a settlement the update would refuse.  A billing period can be prorated more than once. Two limits apply:  - While a settlement is in flight the next one is rejected, and the guard has one-second   granularity, so a repeat inside the same second as the previous change is rejected too.   Retry after at least a second. - A period whose length no longer matches the plan\'s interval cannot be prorated at all   until the next renewal restores it. A `nextPaymentAt` change resizes the period by   definition, and an `interval` or `intervalCount` change without `prorate` leaves the old   period in place under the new interval. Either one closes the period to further prorated   changes, because the credit and the charge are both fractions of the stored period and   would divide by a length the price was never set against.  Accepted on active and trialing subscriptions only. On a trialing subscription the fields change and no money moves, because there is no paid time to credit. Any other status is rejected.  You cannot combine `prorate` with `trialPeriodEnd`, `cancelAtPeriodEnd`, `pauseAtPeriodEnd`, `pauseIntervalCount` or `skipIntervalCount`.  On an active subscription, these are rejected too:  - A subscription that pays with Bizum, or whose `allowedPaymentMethods` include it. - `prorate` without a change to `amount`, `interval`, `intervalCount` or   `nextPaymentAt`, because there is nothing to settle. - An `interval` or `intervalCount` change while `cancelAtPeriodEnd`, `pauseAtPeriodEnd`   or `skipIntervalCount` is pending. An `amount` change is accepted. - A change that would refund, when refunds are disabled for the account.  A `nextPaymentAt` change has its own rules:  - Only on an active subscription. A prorated `nextPaymentAt` change is rejected on a   trialing one, where nothing would settle, and on a past due one, which a plain change   accepts. - Not together with `interval` or `intervalCount`. Those already restart the period from   now, so the new period end would have two answers. - The charge is capped at 24 billing periods. A date far enough ahead to exceed that is   rejected rather than charged. - The clearing value described on `nextPaymentAt` is accepted on an anchor move and on   nothing else. Any other prorated change still rejects those fields outright.
   * @type {boolean}
   * @memberof UpdateSubscriptionRequest
   */
  prorate?: boolean;
  /**
   * The date when the next payment will be made. Measured in seconds since the Unix epoch.  Send it on an update to move the next charge without changing the price. The date must be in the future and no more than five years ahead, and the subscription must be active or past due with a live schedule. A trialing subscription moves with `trialPeriodEnd` instead, and a paused one has to be resumed first.  A pending `skipIntervalCount`, `pauseIntervalCount`, `pauseAtPeriodEnd` or `cancelAtPeriodEnd` blocks the change, because it would turn the charge at the new date into a skipped, paused or cancelled cycle instead of a payment. Clear it in the same request by sending `0`, `null` or `false` for that field.  Moving the date on its own settles no money. Combine it with `prorate` to charge or refund the time that moves.
   * @type {number}
   * @memberof UpdateSubscriptionRequest
   */
  nextPaymentAt?: number;
  /**
   * A set of key-value pairs that you can attach to a resource. This can be useful for storing additional information about the resource in a structured format.
   * @type {object}
   * @memberof UpdateSubscriptionRequest
   */
  metadata?: object;
}
